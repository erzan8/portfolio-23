[build]
  command = "pnpm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NEXT_IMAGE_OPTS = "true"

[[plugins]]
  package = "@netlify/plugin-nextjs"
  [plugins.inputs]
    experimentalRemoteImages = true
